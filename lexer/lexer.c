#include "minishell.h"

/*
 * Tokenizer - Girdi stringini token'lara ayırır
 * Input: Raw string
 * Output: t_token linked list
 */
t_token	*lexer(char *input, t_shell *shell)
{
	t_token	*tokens;

	(void)input;
	(void)shell;
	tokens = NULL;
	// TODO: Implement tokenization
	return (tokens);
}
