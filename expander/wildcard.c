#include "minishell.h"

/*
 * Wildcard expansion - * genişletmesi (BONUS)
 */
char	**expand_wildcard(char *pattern, t_shell *shell)
{
	(void)pattern;
	(void)shell;
	// TODO: Implement wildcard expansion (bonus)
	return (NULL);
}
